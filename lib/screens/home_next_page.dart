import 'package:flutter/material.dart';

class HomeNextPage extends StatelessWidget {
  const HomeNextPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('HomeNext'),
      ),
    );
  }
}
